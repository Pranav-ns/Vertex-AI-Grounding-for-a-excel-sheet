# Vertex-AI-Grounding-for-a-excel-sheet
This Python script uses Gemini API via Vertex AI to complete a medical ADC Excel dataset. It takes the first column value as the key, searches public data with Google grounding, fills the remaining columns, normalises values, handles errors/retries, and saves the completed Excel file.
